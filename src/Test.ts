export {};

const array = [33, "semmi", { nev: "béla" }, true];

const array1: object[] = [{ name: "test1" }, { name: "test2" }];

let object: {
  name: string;
  age: number;
  isOpen: boolean;
};

object = {
  name: "semmi",
  age: 33,
  isOpen: false,
};
